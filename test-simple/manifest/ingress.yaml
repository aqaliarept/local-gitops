apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-app
  labels:
    app: nginx-app
spec:
  ingressClassName: nginx
  rules:
  - host: nginx.localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx-app
            port:
              number: 80
